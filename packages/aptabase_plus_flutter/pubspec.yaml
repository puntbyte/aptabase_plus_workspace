name: aptabase_plus_flutter
description: Flutter integration package for aptabase_plus with lifecycle and system-info adapters.
version: 0.1.0

homepage: https://aptabase.com
repository: https://github.com/aptabase/aptabase_flutter
issue_tracker: https://github.com/aptabase/aptabase_flutter/issues
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.1'

resolution: workspace

dependencies:
  aptabase_plus:
    path: ../aptabase_plus
  device_info_plus: ^13.1.0
  flutter:
    sdk: flutter
  package_info_plus: ^10.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
